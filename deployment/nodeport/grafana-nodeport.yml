---
apiVersion: v1
kind: Service
metadata:
  name: grafana-nodeport
  labels:
    app.kubernetes.io/name: grafana
spec:
  type: NodePort
  ports:
  - name: grafana
    port: 3000
    targetPort: http
    nodePort: 30300
  selector:
    app.kubernetes.io/name: grafana
